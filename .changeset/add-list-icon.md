---
"@jasonyangcis/core-ui": minor
---

Add headless `ListIcon` marketing component: renders a list of icon + label items via a `data-slot="list-icon"` structure. Icon rendering is left to the consumer via an `icon` slot on each item, matching the `imageSlot`/`ctaSlot` pattern already used by `HeroSplit`.
