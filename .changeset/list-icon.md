---
"@jasonyangcis/core-ui": minor
---

Add headless ListIcon component for icon + label feature grids. Ships structure and `data-*` slots only; icon rendering is supplied by the consumer via the `icon` ReactNode prop on each item.
