---
"@jasonyangcis/core-ui": minor
---

Add `ListIcon`, a headless icon-list primitive that resolves Lucide icons by name (via `lucide-react/dynamicIconImports`, code-split per icon) and a matching `ListIcon.builder.ts` config exposing every Lucide icon name as a Builder editor dropdown.
