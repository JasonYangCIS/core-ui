---
"@jasonyangcis/core-ui": minor
---

Add `AnnouncementBar` headless primitive with `AnnouncementBar.builder.ts` for Builder.io registration. Renders a `data-slot="announcement-bar"` root (div or anchor when `href` is set), glyph slots on both sides of the message, and `data-linked="true"` on the anchor variant.
